# Streamlit Community Cloud secrets template.
# In the app's "Advanced settings -> Secrets" box (or a local
# .streamlit/secrets.toml), paste your key. The copilot falls back to templated
# explanations if it is absent, so the app runs either way.
ANTHROPIC_API_KEY = "your_real_key_here"
