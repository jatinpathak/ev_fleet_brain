# Pinned to the exact versions the app was built and tested against, so the
# deployed environment (e.g. Streamlit Community Cloud) reproduces it exactly.
# streamlit is pinned deliberately: 1.59.2 still supports use_container_width,
# avoiding a break from a future release that removes it.
streamlit==1.59.2
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
plotly==6.9.0
anthropic==0.116.0
python-dotenv==1.2.2
pytest==9.1.1
