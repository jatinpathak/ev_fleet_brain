# Copy this file to `.env` and paste your real key.
# The copilot falls back to templated explanations if this is absent,
# so the app never crashes without a key.
ANTHROPIC_API_KEY=your_real_key_here
